{
  "name": "skippy-guy-site",
  "version": "1.0.0",
  "private": true,
  "description": "Short film site for Skippy Guy — static site served by a Cloudflare Worker.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
